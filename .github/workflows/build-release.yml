name: Build and Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g., v1.0.0)"
        required: true
        type: string

jobs:
  build:
    name: Build on ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - os: windows
            runner: windows-latest
            arch: x64
            artifact_name: mcp-server-windows-x64.zip
            dist_path: dist/mcp-server

          - os: linux
            runner: ubuntu-latest
            arch: x64
            artifact_name: mcp-server-linux-x64.tar.gz
            dist_path: dist/mcp-server

          - os: macos
            runner: macos-13
            arch: x64
            artifact_name: mcp-server-macos-x64.tar.gz
            dist_path: dist/mcp-server

          - os: macos
            runner: macos-latest
            arch: arm64
            artifact_name: mcp-server-macos-arm64.tar.gz
            dist_path: dist/mcp-server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run tests
        run: pytest -v
        continue-on-error: false

      - name: Build executable
        run: python scripts/build/build.py --onedir

      - name: Verify build
        run: |
          ls -R ${{ matrix.dist_path }}
        shell: bash

      - name: Copy USAGE.md
        run: |
          cp .github/assets/USAGE.md USAGE.md
        shell: bash

      - name: Package for Windows
        if: matrix.os == 'windows'
        run: |
          Copy-Item USAGE.md ${{ matrix.dist_path }}\USAGE.md
          Compress-Archive -Path ${{ matrix.dist_path }}\* -DestinationPath ${{ matrix.artifact_name }}
        shell: pwsh

      - name: Package for Linux/macOS
        if: matrix.os != 'windows'
        run: |
          cp USAGE.md ${{ matrix.dist_path }}/USAGE.md
          tar -czf ${{ matrix.artifact_name }} -C dist mcp-server
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}
          retention-days: 7

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure
        run: ls -R artifacts

      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "version=${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Generate release notes
        run: |
          # Dynamically extract tool counts from plugin config.yaml files
          TOTAL_TOOLS=0
          TOOL_LINES=""

          declare -A ICONS
          ICONS[file]="📁"
          ICONS[compression]="🗜️"
          ICONS[data]="📊"
          ICONS[text]="📝"
          ICONS[web]="🌐"
          ICONS[system]="💻"
          ICONS[utility]="🔧"
          ICONS[subagent]="🤖"

          for config in src/mcp_server/tools/*/config.yaml; do
            plugin_dir=$(dirname "$config")
            plugin_name=$(basename "$plugin_dir")
            handlers="$plugin_dir/handlers.py"

            # Parse category_name from config.yaml
            category_name=$(python3 -c "import yaml; c=yaml.safe_load(open('$config')); print(c.get('category_name',''))")

            # Count @tool_handler decorated functions in handlers.py
            tool_count=0
            if [ -f "$handlers" ]; then
              tool_count=$(grep -c '@tool_handler' "$handlers" || true)
            fi

            if [ "$tool_count" -gt 0 ] 2>/dev/null; then
              TOTAL_TOOLS=$((TOTAL_TOOLS + tool_count))
              icon="${ICONS[$plugin_name]:-🔹}"
              TOOL_LINES="${TOOL_LINES}
          - ${icon} **${category_name}** (${tool_count} tools)"
            fi
          done

          # Count categories
          TOTAL_CATEGORIES=$(echo "$TOOL_LINES" | grep -c "^-" || true)

          cat > RELEASE_NOTES.md << EOL
          ## MCP Server Release ${{ steps.get_version.outputs.version }}

          ### 📦 Available Downloads | 可用下载

          Choose the appropriate package for your platform:
          为您的平台选择合适的包：

          - **Windows (x64)**: \`mcp-server-windows-x64.zip\`
          - **Linux (x64)**: \`mcp-server-linux-x64.tar.gz\`
          - **macOS (Intel)**: \`mcp-server-macos-x64.tar.gz\`
          - **macOS (Apple Silicon)**: \`mcp-server-macos-arm64.tar.gz\`

          Each package includes:
          每个包包含：
          - Pre-built executable | 预构建的可执行文件
          - USAGE.md with setup instructions | 包含设置说明的USAGE.md

          ### ✨ Features | 功能

          This release includes **${TOTAL_TOOLS} tools** across ${TOTAL_CATEGORIES} categories:
          此版本包含${TOTAL_CATEGORIES}个类别的**${TOTAL_TOOLS}个工具**：
          ${TOOL_LINES}

          ### 🚀 Quick Start | 快速开始

          #### 1. Download & Extract | 下载解压

          Download the package for your platform and extract it:
          下载适合您平台的包并解压：

          **Windows:**
          \`\`\`powershell
          Expand-Archive -Path mcp-server-windows-x64.zip -DestinationPath C:\mcp-server
          \`\`\`

          **Linux/macOS:**
          \`\`\`bash
          tar -xzf mcp-server-*.tar.gz
          \`\`\`

          #### 2. Configure MCP Client | 配置 MCP 客户端

          <details>
          <summary><b>Claude Desktop</b></summary>

          Edit your config file (create if not exists):
          编辑配置文件（不存在则创建）：

          - **Windows:** \`%APPDATA%\Claude\claude_desktop_config.json\`
          - **macOS:** \`~/Library/Application Support/Claude/claude_desktop_config.json\`
          - **Linux:** \`~/.config/Claude/claude_desktop_config.json\`

          **Windows example:**
          \`\`\`json
          {
            "mcpServers": {
              "mcp-server": {
                "command": "C:\\\\mcp-server\\\\mcp-server\\\\mcp-server.exe"
              }
            }
          }
          \`\`\`

          **macOS/Linux example:**
          \`\`\`json
          {
            "mcpServers": {
              "mcp-server": {
                "command": "/path/to/mcp-server/mcp-server"
              }
            }
          }
          \`\`\`

          > ⚠️ Use **absolute paths** only. After saving, fully quit and restart Claude Desktop.
          > ⚠️ 必须使用**绝对路径**。保存后完全退出并重启 Claude Desktop。

          </details>

          <details>
          <summary><b>Claude Code (VS Code / CLI)</b></summary>

          \`\`\`bash
          claude mcp add mcp-server /absolute/path/to/mcp-server/mcp-server
          \`\`\`

          Or add to VS Code \`settings.json\`:
          或添加到 VS Code \`settings.json\`：

          \`\`\`json
          {
            "claude.mcpServers": {
              "mcp-server": {
                "command": "/absolute/path/to/mcp-server/mcp-server"
              }
            }
          }
          \`\`\`

          </details>

          <details>
          <summary><b>Cursor</b></summary>

          Edit \`~/.cursor/mcp.json\` (global) or \`<project>/.cursor/mcp.json\` (project-level):
          编辑 \`~/.cursor/mcp.json\`（全局）或 \`<project>/.cursor/mcp.json\`（项目级）：

          \`\`\`json
          {
            "mcpServers": {
              "mcp-server": {
                "command": "/absolute/path/to/mcp-server/mcp-server"
              }
            }
          }
          \`\`\`

          </details>

          <details>
          <summary><b>Other MCP Clients | 其他客户端</b></summary>

          Any MCP-compatible client can use this server. You typically need to provide the **absolute path** to the executable as the \`command\` field.
          任何兼容 MCP 的客户端都可以使用此服务器，只需将可执行文件的**绝对路径**作为 \`command\` 字段。

          </details>

          #### 3. Verify | 验证

          Ask your AI assistant: *"What MCP tools are available?"*
          向你的 AI 助手提问：*"有哪些可用的 MCP 工具？"*

          ### 📚 Documentation | 文档

          - [Complete Documentation](https://github.com/${{ github.repository }}/tree/main/docs)
          - [Build Guide](https://github.com/${{ github.repository }}/blob/main/docs/BUILD.md)
          - [Architecture](https://github.com/${{ github.repository }}/blob/main/docs/ARCHITECTURE.md)

          ### 🐛 Bug Reports | Bug报告

          Found an issue? Please [create a bug report](https://github.com/${{ github.repository }}/issues/new?template=bug_report.yml).
          发现问题？请[创建bug报告](https://github.com/${{ github.repository }}/issues/new?template=bug_report.yml)。
          EOL

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: Release ${{ steps.get_version.outputs.version }}
          body_path: RELEASE_NOTES.md
          draft: true
          prerelease: false
          files: |
            artifacts/mcp-server-windows-x64.zip/mcp-server-windows-x64.zip
            artifacts/mcp-server-linux-x64.tar.gz/mcp-server-linux-x64.tar.gz
            artifacts/mcp-server-macos-x64.tar.gz/mcp-server-macos-x64.tar.gz
            artifacts/mcp-server-macos-arm64.tar.gz/mcp-server-macos-arm64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
